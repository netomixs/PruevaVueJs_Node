export default class Usuario {
    constructor(id,Numero,Calle,Cp,Ciudad,Estado) {
     this.id = id;
     this.Numero = Numero
     this.Calle = Calle;
     this.Cp=Cp;
     this.Ciudad = Ciudad;
     this.Estado = Estado;
  }
}
  